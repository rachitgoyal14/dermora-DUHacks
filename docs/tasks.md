1. remove clerk authentication, use manual auth instead
2. use cloudflare R2 bucket for image uploads, rather than storing the images inside the backend/uploads/ directory
3. fix the solace voice cracking issue => whenuser 
4. make the app faster, imporve its UI
5. dockerize and deploy the backend on GCP

